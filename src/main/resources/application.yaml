spring:
  application:
    name: neobank-backend

  datasource:
    url: jdbc:mysql://localhost:3306/neobank?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: Manoj@9861
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  devtools:
    restart:
      enabled: true

# --- JWT Configuration ---
jwt:
  secret: 3f8a2b1d9e4c7f0a6b3d8e2c5a9f1b4d7e0c3a6f9b2d5e8a1c4f7b0d3e6a9c20
  expiration: 86400000

server:
  port: 8080

logging:
  level:
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG